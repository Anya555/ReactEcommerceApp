import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
  root: {
    maxWidth: 300,
    height: "300px",
  },

  media: {
    paddingTop: 50,
    height: "100px",
  },
  cart: {
    color: "red",
  },
});
