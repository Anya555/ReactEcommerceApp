import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
  root: {
    maxWidth: 300,
  },
  cart: {
    color: "red",
  },
  main: {
    marginTop: "50px",
  },
});
