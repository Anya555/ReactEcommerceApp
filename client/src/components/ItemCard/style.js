import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
  root: {
    maxWidth: 300,
  },
  main: {
    marginTop: "50px",
  },
  learn: {
    textDecoration: "none",
    color: "black",
  },
});
